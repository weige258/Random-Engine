set(CMAKE_RUNTIME_OUTPUT_DIRECTORY ${CMAKE_BINARY_DIR})

add_subdirectory(Libs)
add_subdirectory(Platform)
add_subdirectory(Core)
add_subdirectory(Systems)
add_subdirectory(Engine)

add_executable(RandEngine main.cpp)

target_include_directories(RandEngine PRIVATE ${CMAKE_CURRENT_SOURCE_DIR})

target_link_libraries(RandEngine PRIVATE Engine)
target_link_libraries(RandEngine PRIVATE Core)